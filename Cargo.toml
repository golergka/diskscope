[package]
name = "diskscope"
version = "0.1.0"
edition = "2021"
description = "Low-memory disk usage scanner inspired by Disk Inventory X"
license = "MIT"

[dependencies]
crossbeam-channel = "0.5"
eframe = { version = "0.29", default-features = true }
globset = "0.4"
libc = "0.2"
