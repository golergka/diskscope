# Legacy Web Prototype

This folder contains the earlier web prototype treemap viewer.

Primary interface is now the native app:

```bash
./target/release/diskscope ui
```

You can still generate snapshots from CLI:

```bash
./target/release/diskscope scan /path --snapshot /tmp/scan.bin
```

The web prototype is kept for reference only.
